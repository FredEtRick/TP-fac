#ifndef ObjSign
#define ObjSign

#include <iostream>
#include <string>

using namespace std;

class ObjSign : virtual public ObjMus
{
	protected :
		String nomAuteur const;
	public :
		ObjSign();
		ObjSign(String, int, String);
		String getNomAuteur() const;
		void setNomAuteur(String);
		void saisie(&istream const);
		void affiche(&osream) const;
}

#endif
